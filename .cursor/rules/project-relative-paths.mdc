---
description: Always use project-relative paths (@/...) instead of relative paths (./ or ../)
alwaysApply: true
---

# Import paths

See **docs/Rules.md → "Import paths"** for the full rule.

**Always use `@/` paths** (mapped to `src/` in `tsconfig.json`). Never use relative imports (`./` or `../`).

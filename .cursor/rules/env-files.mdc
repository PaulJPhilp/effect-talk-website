---
description: How env files work and rules to manage them (see docs/env.md)
alwaysApply: true
---

# Environment files

See **docs/Rules.md → "Environment files"** for the full rule.

**Do not modify `.env`, `.env.example`, or `.env.local` unless the user explicitly asks.** See also `docs/env.md`.
